# 운영체제 관련 불필요한 파일
__pycache__/
*.pyc
*.pyo

# 환경 변수 파일 (보안)
.env

# 가상환경 폴더 (로컬 개발용)
venv/
.virtualenv/

# Git 관련 파일 (필요 없음)
.git/
.gitignore

# Docker 자체 파일 (이미 존재하는 것)
Dockerfile
docker-compose.yml
